namespace CarManufactureAPI.Models
{
    /// <summary>
    /// Enum que representa los diferentes modelos de automóviles disponibles.
    /// Cada modelo tiene un precio base asociado que se define en la clase CarModelPricing.
    /// </summary>
    public enum CarModelType
    {
        Sedan,
        SUV,
        Offroad,
        Sport
    }
}
